{
  "name": "automated-farming-drone",
  "version": "1.0.0",
  "description": "Smart contracts for automated farming drone system",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@blockstack/clarity": "^0.3.0"
  },
  "devDependencies": {
    "vitest": "^1.6.0",
    "typescript": "^5.0.0",
    "@types/node": "^18.0.0"
  }
}
